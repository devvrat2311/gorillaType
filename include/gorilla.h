#include <stdio.h>

int getlineCustom(char line[], int maxlen); //This is a custom function written by the user to use getchar() in a for loop to get a line entry as a whole
void copy(char to[], char from[]); //This basically copies the string from[] to the string to[]
int getLineNoTrailingSpaces(char line[], int maxlen);//no trailing spaces ' ' -> question 1-18 from c book. the same function as getlineCustom but this removes any trailing spaces from the input,

void todolist(void);//entry point for the to do list app


